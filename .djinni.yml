default_model: qwen-qwen3-coder-next-fp8
agents:
  local-agent:
    harness_command:
      - bash
    containerfile: ./Containerfile
    mounts:
      - source: /home/jgruen/.config/opencode
        destination: /home/jgruen/agent/.config/opencode
  registry-agent:
    harness_command:
      - python
      - -m
      - agent.harness
    image: python:3.11-slim
    mounts:
      - source: ./specs
        destination: /app/specs
  git-agent:
    harness_command:
      - python
      - -m
      - agent.harness
    containerfile: ./Containerfile
    git_workspace:
      base_directory: /tmp/git-agent
    mounts:
      - source: ./spec
        destination: /app/specs
