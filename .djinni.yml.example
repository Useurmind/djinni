agents:
  coding-agent:
    harness_command:
      - python
      - -m
      - agent.harness
    image: python:3.11-slim
    mounts:
      - source: ./models
        destination: /app/models
      - source: ~/.config/creds
        destination: /app/creds
        readOnly: true
