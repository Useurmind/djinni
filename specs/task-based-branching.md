After the agent container exits the branch should be pushed to the original repository.
Also when the docker container exits, all changes should be commited to the branch.
Use the configured ai model to 